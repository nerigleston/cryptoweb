const CalculatorResult = ({ totalValue }) => (
  <div className="mt-10">
    <p className="text-center text-2xl font-bold">Total em Reais: R${totalValue}</p>
  </div>
);

export default CalculatorResult;
